{
  "name": "phoenix-forensic-toolkit",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:ui": "vite build",
    "lint": "oxlint",
    "preview": "vite preview",
    "electron:dev": "concurrently -k \"npm run dev\" \"wait-on http://localhost:5173 && cross-env NODE_ENV=development npx electron .\"",
    "build:backend:win": "pyinstaller --noconfirm --clean --onefile --name forensic-backend --distpath ../backend/dist --workpath ../backend/build/windows --specpath ../backend/build/specs --paths .. --paths ../backend --hidden-import uvicorn.logging --hidden-import uvicorn.loops.auto --hidden-import uvicorn.protocols.http.auto --hidden-import uvicorn.lifespan.on --hidden-import api.main --hidden-import pydantic --hidden-import hashlib --hidden-import hmac --hidden-import fastapi ../backend/run_server.py",
    "build:backend:linux": "pyinstaller --noconfirm --clean --onefile --name forensic-backend --distpath ../backend/dist --workpath ../backend/build/linux --specpath ../backend/build/specs --paths .. --paths ../backend --hidden-import uvicorn.logging --hidden-import uvicorn.loops.auto --hidden-import uvicorn.protocols.http.auto --hidden-import uvicorn.lifespan.on --hidden-import api.main --hidden-import pydantic --hidden-import hashlib --hidden-import hmac --hidden-import fastapi ../backend/run_server.py",
    "build:backend": "node electron/build-backend.cjs",
    "pack:win": "npm run build:ui && npm run build:backend:win && electron-builder --config electron-builder.json --win --dir",
    "pack:linux": "npm run build:ui && npm run build:backend:linux && electron-builder --config electron-builder.json --linux --dir",
    "package:win": "npm run build:ui && npm run build:backend:win && electron-builder --config electron-builder.json --win",
    "package:linux": "npm run build:ui && npm run build:backend:linux && electron-builder --config electron-builder.json --linux",
    "dist": "npm run build:ui && npm run build:backend && electron-builder --config electron-builder.json",
    "electron:build": "npm run dist"
  },
  "dependencies": {
    "@rolldown/binding-linux-x64-gnu": "^1.2.8",
    "@tabler/icons-react": "^3.46.0",
    "lucide-react": "^1.41.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-router-dom": "^7.18.3",
    "recharts": "^3.10.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.1.0",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron": "^34.2.0",
    "electron-builder": "^25.1.8",
    "oxlint": "^1.79.0",
    "tailwindcss": "^4.3.3",
    "vite": "^8.2.2",
    "wait-on": "^8.0.2"
  }
}
